HereHere is the complete, comprehensive, and non-truncated **`README.md`** documentation file for your repositories, generated in a single code block for is the complete, high-end, and comprehensive **`README.md`** file for your project, written to easy copy-pasting into your project's root directory:

```markdown
# SmartFlow AI | Autonomous Smart Money Tracker
 professional, open-source engineering standards. 

It is generated entirely inside a single, non-truncated code block for immediate copy-pasting into your root repository directory:

```markdown
# SmartFlow AI Terminal
> The Bloomberg of On-Chain Intelligence for Retail Traders. Built for Bitget AI Base Camp Hackathon S1.

SmartFlow AI is an autonomous blockchain intelligence platform engineered to track, evaluate, and interpret the behaviors of smart money participants (institutions, venture capitals, elite wallets) in real-time. By combining raw on-chain transaction streams, semantic web search indices, and large language model (LLM) reasoning, the system converts complex multi-chain data into actionable market> Institutional On-Chain Intelligence for Retail Traders — Powered by Aliyun Qwen & Bitget

SmartFlow AI is an autonomous, production-grade blockchain intelligence platform engineered to track, evaluate, and interpret high-volume on-chain transactions (smart money, institutions, elite wallets) in real-time. By combining a resilient mainnet block scanner, Aliyun Qwen-Plus model rotations intelligence delivered through an ultra-premium React dashboard and an interactive, persistent-keyboard Telegram Bot.

---

## 1., Tavily semantic web sweeps, and a secure Supabase Cloud PostgreSQL database, the platform translates raw blockchain logs into structured System Vision & Executive Summary

The cryptocurrency market operates 24/7 across multiple blockchains, where large institutional investors, venture capital funds, and high-performing on-chain traders accumulate positions before news becomes public, rotate capital across emerging sectors, and exit positions before major visual briefings delivered simultaneously across an ultra-premium React 3D glassmorphism dashboard and an interactive Telegram dApp.

---

## 🤖 corrections. Retail traders usually discover these movements too late because current analytics solutions are often expensive, overly technical, or focused on raw transactional Platform Core Capabilities

### 1. Hybrid WebSocket & JSON-RPC Block Scanner
* **Sub-Second Scanning**: Connects via persistent TCP WebSockets (`wss://`) to mainnet Ethereum nodes to scan transaction blocks instantly as they are minted on data instead of actionable semantic intelligence.

**SmartFlow AI** bridges this asymmetry by delivering institutional-grade, AI-synt-chain.
* **Failover Resilience**: Features an automated fallback connection manager. If the WebSocket stream encounters timeouts or resetshesized on-chain intelligence. The platform:
1. **Listens to Live Blockchains**: Employs real-time WebSocket, the system seamlessly transitions to standard HTTP JSON-RPC polling across a pool of public nodes (Cloudflare, PublicNode, LlamaRPC), keeping your trackers online 24/7.
* **Multi-Asset Token Scanners**: Tracks native ETH transfers and parses ERC-20 contract event logs (such as USDT, USDC, LINK, ONDO, BGB, and BNB) using single-call block log filtering to prevent RPC rate-limits.

### 2. Aliyun Qwen-Plus Adaptive event listeners to scan Ethereum blocks instantly as they are minted on-chain.
2. **Filters for Million-Dollar Whales**: Enforces a dynamic, real-time USD-valuation filter that completely eliminates dust spam, only capturing high-conviction transfers exceeding $1,000,000 USD.
3. **Executes AI Semantic Interpretations**: Pairs Model Rotations
* **Model Rotation Gateways**: Automatically bypasses local or regional trial quota limits by rotating requests across `qwen-turbo`, `qwen-plus`, and `qwen-max` completions.
* **95%+ transactions with real-time global news contexts scraped via Tavily, and dispatches them to Alibaba Qwen-Plus (utilizing custom model-rotation fallbacks) to compute confidence, expected impact, and write clear, human-readable explanations.
4. **Persists Cloud History**: Stores all scanned transactions in a live, persistent cloud PostgreSQL database on Supabase.
5. **Autonomously Broadcast Token Conservation Layer**: Employs an on-chain value gatekeeper. Standard transactions bypass Qwen entirely and are evaluated using clean, static code templates, while Aliyun's LLM completions are strictly reserved for high-impact million-dollar transfers ($\ge \$1,000,000$ USD).
* **Strict Schema Validation**: Binds completions using rigid JSON schemas to guarantee parsed outputs Alerts**: Instantly pushes these scored transaction cards to Telegram users who have enabled live alerts, creating a proactive notification sentinel.
6. **Autonomously Publishes Research**: Runs a background scheduler every 6 hours to aggregate historical transactions, analyze current market trends, write keys align with TypeScript types, preventing parsing exceptions.

### 3. Persistent Supabase Cloud Database
* **PostgreSQL Schema a Bloomberg-style macroeconomic newsletter, publish it to the dashboard, and broadcast it to subscribers.

---

## 2.**: Stores and serves persistent smart money historical logs, subscription registries, and macro research reports.
* **Decoupled REST Queries**: Interfaces with Supabase's native REST Data API over standard HTTP, removing heavy database engine drivers and ensuring zero compile-time dependencies.

### 4. Interactive Telegram Bot & Persistent Menus
* **Collapsible Keyboard Layout**: Renders an organic Core Architecture & Data Flow

```
[ Active Mainnet Block Minted ]
               │
               ▼
[ WebSocket Scanner Parses Block on Render ]
               │
               ▼
[ Filters Transactions >= $1,000,000 USD ]
               │
               ▼
[ Aliyun Qwen Analyzes & Scores Transaction ]
               │
               ▼
[ Scanned Alert Written Persistently to Supabase PostgreSQL ]
               │
               ▼
[ Server Queries Active Subscriber Chat IDs from Supabase ]
               , collapsible grid reply menu (`🐳 Monitored Wallets`, `📈 Bitget Spot Balances`, `💡 Narrative Insights`, `📊│
               ▼
[ Telegraf Bot Broadcasts Formatted HTML Alert Card Instantly ]
```

### Key Architectural Sector Rotations`, `🔔 Enable Alerts`, `⚡ System Status`, `ℹ️ Help Guide`), matching premium consumer dApp designs.
* **Parallel Integration Layers:
*   **The WebSocket Scanner & HTTP Fallback**: Connects directly to mainnet Ethereum nodes over Webized Blockscout Balance Scanner**: Queries current, on-chain balances and live fiat valuations of monitored addresses dynamically using BlockSockets (`wss://`) for sub-second block streaming. If the socket connection drops due to gateway timeouts, the enginescout's public indexer, bypassing typical cloud RPC datacenter rate-limit blocks.
* **Dynamic Multi catches the event and instantly activates a graceful, multi-node HTTP JSON-RPC polling loop.
*   **Dynamic-Chain Predictor (`/prediction [coin]`)**: Dispatches autonomous research agents to sweep Tavily search indexes and write custom Aliyun Model Rotation**: Outgoing LLM requests are processed over secure international Aliyun gateways. If a model encounters technical bias, support, and resistance forecasts for any coin (BTC, SOL, ETH, BGB) in real-time trial quota exhaustion or rate limits, the service automatically rotates to secondary models (`qwen-turbo` → `qwen-plus` → `qwen-max`), guaranteeing 24/7 completions uptime.
*   **95%+ Token Conservation.
* **Proactive Broadcast Alerts**: Stores user chat IDs upon clicking `🔔 Enable Alerts` and broadcasts scanned million Layer**: To protect API quotas and limit billing costs, the block scanner runs dynamic on-chain filtering. It generates native, highly optimized templates for standard transactions, and only invokes Aliyun Qwen LLM completions for high-profile whale events.
*   **Parallel-dollar transactions as rich HTML cards with Etherscan deep-linking.

### 5. Autonomous Bitget Copy-Trading Agent
* **Agentic Execution**: Monitors active on-chain buying spikes. If a watched elite wallet executes a high-confidence asset Blockscout REST Indexer**: To query monitored wallet balances on Telegram without triggering datacenter IP rate-limits (common accumulation on-chain, the backend autonomously triggers a matching Spot market buy order on your Bitget account in sub-second real-time.
* **Dynamic Time-Drift Calibrations**: Regularly syncs local clock offsets against Bitget's central server on public JSON-RPC nodes), the bot queries Blockscout's public REST API in parallel. This bypasses rate-limit blocks and fetches live USD balance conversions.
*   **Bitget Time-Drift Sync Calibration**: Private public clock (`GET /api/v2/public/time`) to compensate for VPS clock drift, preventing access signature Bitget API endpoints require precise cryptographic access headers. SmartFlow AI actively queries Bitget's central server clock, calculates expirations.

### 6. 6-Hour Autonomous Research Scheduler
* **AI Research Desk**: A background scheduler running local clock drift, and applies this offset to all Access-Timestamp headers, ensuring signature validity on virtualized containers.

---

##  every 6 hours to fetch your last 50 scanned on-chain database logs, aggregate capital inflow categories, sweep macroeconomic3. Technology Stack

### Frontend Client
*   **React (v18+) & Vite (v6+)**: Core single-page application framework.
*   **TypeScript (v5+)**: Enforces strict type-safety across all components.
*   **Tailwind CSS (v4+) & PostCSS**: Renders the dark-theme news via Tavily, write a Bloomberg-standard market report, write it to Supabase, and broadcast it to your Telegram subscribers.

---

## 📊 System Architecture Map

```mermaid
graph TD
    UserWeb[React Web App] -->|, translucent ashes-and-slate Apple-standard frosted glassmorphic UI.
*   **Privy React SDK**: Multi-methodHTTPS Requests| ExpressAPI[Express REST Server]
    UserTG[Telegram Client] -->|Slash Commands / Buttons| TelegramBot[Telegraf Bot Engine]

    subgraph Backend Core [Node.js Express App]
        ExpressAPI --> apiRouter Web3 wallet, Google Social, and Email authentications.
*   **Lucide React & Axios**: Interface iconography[API Router /routes/api.ts]
        apiRouter -->|GET /signals| SignalState[Signal State Manager]
        apiRouter -->|GET /bitget/assets| BitgetService[Bitget REST Service]
        apiRouter -->|GET /sectors/weights| QwenService[Qwen LLM Service]
        apiRouter -->|GET /research| ResearchAgent[Autonomous Research Agent]
    end

     and dynamic REST integrations.

### Backend Server
*   **Node.js (v22+) & Express**: Core RESTsubgraph Telegram Agent [Telegraf Engine]
        TelegramBot -->|🐳 Monitored Wallets| BlockchainTracker API router.
*   **Ethers.js (v6+)**: On-chain block polling and cryptographic decoding.
*   **Telegraf (v4+)**: Telegram Bot client and interface wrapper.
*   **Supabase Cloud**: Persistent PostgreSQL hosting and REST Data API endpoints.
*   **Axios, Helmet, Morgan**: Client requests, secure HTTP headers, and request logging.

---

[Blockchain Tracker Service]
        TelegramBot -->|📈 Bitget Spot Balances| BitgetService
        TelegramBot -->|📊 Sector Rotations| TavilyService[Tavily Search Service]
        TelegramBot -->|💡 Narrative Insights| Tav## 4. Project Folder Directory Tree

```
AutonomousSmartMoneyTracker/
├── backend/
│   ├── src/
│   │   ├── config/
│   │   │   └── index.ts          # Sanitizes, trims, and validates envilyService
        TavilyService -->|Web Context| QwenService
    end

    subgraph Block Scanner [Ethers Engine]
        BlockchainTracker -->|getProvider| RPCFallback variables
│   │   ├── routes/
│   │   │   └── api.ts            # REST endpoints[Resilient RPC Fallback Pool]
        RPCFallback -->|New Block Event| BlockScannerLoop[Block Scanner Loop]
        BlockScannerLoop -->|Filter tx >= $1,000,000 USD| BlockScannerLoop
        BlockScannerLoop -->|Match Monitored Wallet| AI (/signals, /health, /research)
│   │   ├── services/
│   │   │   ├── bitget.ts         Review[Trigger AI Evaluation]
        AIReview --> QwenService
        AIReview -->|Push Signal| Signal# Base64 HMAC spot trade signer & REST client
│   │   │   ├── dexscreener.ts    # Real-time token pool price lookups
│   │   │   ├── mulerun.ts        # MuleRun AgentState
        AIReview -->|Broadcast Alert| TelegramBot
    end

    BitgetService -->|Base64 HMAC Signature| BitgetAPI((Bitget spot API V2))
    QwenService -->|Bearer Authentication| QwenAPI((Aliyun Dashscope-Intl))
    RPCFallback -->|Multi-RPC Queries| EthChain((Ethereum Mainnet Node))
```

---

## 📂 File System Directory Tree

```
AutonomousSmartMoneyTracker/
├── backend/ platform integrations
│   │   │   ├── qwen.ts           # Aliyun completions with model-rotation

│   ├── src/
│   │   ├── config/
│   │   │   └── index.│   │   │   ├── researchAgent.ts  # 6h Autonomous macro research compiler
│   │   │   ├──ts          # Environment loading, validation, and sanitization
│   │   ├── routes/
│   │   │   └── api.ts             signalState.ts    # Supabase PostgreSQL database manager
│   │   │   ├── tavily.ts         # Tav# Modular router and native Express IP rate-limiter
│   │   ├── services/
│   │   │   ├── bitget.ily semantic context search with 1h cache
│   │   │   ├── telegram.ts       # Bot controllers & Blockscout REST query engine
│   │   │   └── tracker.ts        # WebSocket block scan loop &ts         # Base64 HMAC spot trading signer and public clock drift syncer
│   │   │   ├── dexscreener.ts     ERC-20 event filter
│   │   ├── types/
│   │   │   └── index.ts          # Real-time token pool parser
│   │   │   ├── mulerun.ts        # MuleRun agent platform connector
│   │   │   ├── qwen.ts           # Aliyun model completions with automated model rotations# Unified TypeScript interfaces
│   │   └── index.ts              # Server entry, boot sequences & daily scheduler
│   ├── tsconfig.json             # TypeScript compiler settings
│   ├── package.json              # Backend dependencies & script definitions
│   └──
│   │   │   ├── researchAgent.ts  # Autonomous 6-hour macro researcher
│   │   │   ├── signalState.ts    # Supabase cloud database persistent state manager
│   │   │   ├── tavily.ts         # .env                      # Local environment keys (ignoring on Git)
└── frontend/
    ├── src/
    │   ├── components/
    │   │   └── GlassLogo.tsx     # 3D CSS-rendered liquid Semantic web searcher with 1-hour cache layer
│   │   │   ├── telegram.ts       # Persistent glowing brand logo
    │   ├── App.tsx               # Main Liquid Glass dashboard, modals & Research Desk
    │   ├── main.tsx              # React mounting and Privy Provider injection
    │   └── index.css             # Tailwind v4 import & custom glass reply keyboard bot client and indexer balances
│   │   │   └── tracker.ts        # WebSocket block poller and million-dollar contract log decoders
│   │   ├── types/
│   │   │   └── index.ts          # Shared keyframes
    ├── index.html                # App root html shell with dark theme defaults
    ├── postcss.config.js unified TypeScript types
│   │   └── index.ts              # Express API node entry and scheduler triggers
│   ├── tsconfig.json             # Backend typescript options
│   └── package.json              # Backend dependency registry
└── frontend/
    ├── src/
    │   ├── components/
    │   │   └── GlassLogo.tsx     # Custom CSS-rendered 3D         # Tailwind v4 PostCSS compilation wrapper
    ├── tailwind.config.js        # Core design configurations
    ├── .npmrc                    # Enforces legacy-peer-deps automatically on cloud build
    ├── tsconfig.json             # Global typescript configurations
    └── package.json              # Frontend dependencies & build scripts
```

---

## 5. Environment Variable Configurations

Copy reflective logo
    │   ├── App.tsx               # Apple-standard dark-theme frosted slate client dashboard
    │   ├── main the template below to create your local `.env` configuration file inside your `/backend` directory.

```env
# Server Configuration
PORT=5000
NODE_ENV=development

# Telegram Bot Token
TELEGRAM_BOT_.tsx              # React mounting root and Privy Provider setups
    │   └── index.css             # Tailwind v4 directives and fluid gradient animations
    ├── index.html                # Entry HTML mount with translucent dark themes
    ├── postcss.config.jsTOKEN=your_telegram_bot_token

# Bitget API Credentials (Read/Trade permissions)
BITGET_API_KEY=your_bitget_api_key
BITGET_SECRET_KEY=your_bitget_secret_         # PostCSS compiler parameters
    ├── tailwind.config.js        # Tailwind v4 configuration file
    ├── tsconfig.json             # Client typescript options
    └── package.json              # Client dependency registry
```

---

## 🛠 Databasekey
BITGET_PASSPHRASE=your_bitget_passphrase

# AI Models & Context Search Keys
MULERUN_API_KEY=your_mulerun_api_key
TAVILY_API_KEY=your_tavily_api_key
QWEN_API_KEY=your_aliyun_qwen_api Schema Configuration

To set up your Supabase PostgreSQL database schemas, paste and run this SQL inside your [Supabase SQL Editor](https://supabase_key

# Supabase Cloud Database Credentials
SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=.com/dashboard/project/rzlwtjutaxtvfzctrsnp/sql/new):

```sql
-- Disableyour_supabase_anon_publishable_key

# Blockchain Provider RPC Nodes (Fallback HTTP Endpoint)
ETH_MAINNET_RPC=https://cloudflare-eth.com

# Dynamic Configurable Alert Threshold (Value in USD)
MIN_ALERT_USD=100000 Row-Level Security on current public schemas
ALTER TABLE IF EXISTS public.signals DISABLE ROW LEVEL SECURITY;
ALTER TABLE IF EXISTS public.subscribers DISABLE ROW LEVEL SECURITY;
ALTER TABLE IF EXISTS public.research_reports DISABLE ROW LEVEL SECURITY;

--0
```

---

## 6. Local Installation & Development Launch

To compile and launch the entire stack on your local machine, 1. Create persistent Signals table
CREATE TABLE IF NOT EXISTS public.signals (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    transaction_hash TEXT NOT NULL UNIQUE,
    wallet_label TEXT NOT NULL,
    wallet_category TEXT NOT NULL,
    action TEXT NOT NULL,
    asset TEXT NOT NULL,
    amount TEXT NOT NULL,
    confidence_score INT NOT NULL,
    impact_score INT NOT NULL,
    risk_score INT NOT NULL,
    ai_explanation TEXT NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
); execute these commands step-by-step:

### 1. Database Setup (Supabase)
Log in to your Sup

-- 2. Create persistent Subscribers table (Stores user Chat IDs)
CREATE TABLE IF NOT EXISTS public.subscribers (
    chat_id TEXT PRIMARY KEY,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- 3.abase SQL Editor and execute these DDL queries to create the required tables and disable Row-Level Security:
```sql
CREATE TABLE IF NOT EXISTS public.signals (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    transaction_ Create persistent Research Reports table
CREATE TABLE IF NOT EXISTS public.research_reports (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title TEXT NOT NULL,
    report_text TEXT NOT NULL,
hash TEXT NOT NULL UNIQUE,
    wallet_label TEXT NOT NULL,
    wallet_category TEXT NOT NULL,
    action TEXT NOT NULL,
    asset TEXT NOT NULL,
    amount TEXT NOT NULL,
    confidence_score INT NOT NULL,
    impact_score INT NOT NULL,
    risk_score INT NOT NULL,
    ai_explanation TEXT NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS public.subscribers (
    chat_id TEXT PRIMARY KEY,
    created_at TIMESTAMPTZ    sentiment_rating TEXT NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Index creation timestamps for high-speed dynamic fetches
CREATE INDEX IF NOT EXISTS idx_signals_created ON public.signals(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_signals_hash ON public.signals(transaction_hash);
CREATE INDEX IF DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS public.research_reports (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title TEXT NOT NULL,
    report_text TEXT NOT NULL,
 NOT EXISTS idx_reports_created ON public.research_reports(created_at DESC);
```

---

## 🗝    sentiment_rating TEXT NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

ALTER TABLE public.signals DISABLE ROW LEVEL SECURITY;
ALTER TABLE public.subscribers DISABLE ROW LEVEL SECURITY;
ALTER Environmental Variables Audit

Create a secure `.env` file inside your `/backend` root directory using this template:

```env
PORT= TABLE public.research_reports DISABLE ROW LEVEL SECURITY;
```

### 2. Launch the Backend Server
Navigate to the `/backend` folder, install the packages, and boot the server:
```powershell
cd backend
npm install --legacy-peer-deps5000
NODE_ENV=development

# Telegram Bot Configurations
TELEGRAM_BOT_TOKEN="your-telegram-bot-token"

# Bitget V2 API Access Credentials
BITGET_API_KEY="your-bitget-api-key"
BITGET_SECRET_KEY="your-bitget-secret-key"

npm run build
npm run dev
```

### 3. Launch the Frontend Dashboard
Open a new terminal, navigate to `/frontend`, and boot the Vite development server:
```powershell
cd frontend
npm install --legacy-peer-deps
npm run dev
```
Open **`http://localhost:5173/`** in yourBITGET_PASSPHRASE="your-bitget-passphrase"

# Integration Platform API Keys
MULERUN_API_KEY="your-mulerun-api-key"
TAVILY_API_KEY="your-tavily-api-key"
QWEN_API_KEY="your-aliyun-qwen-api-key browser. The responsive, dark-theme 3D liquid glassmorphic client will load and sync with your local Node API.

---

## 7. Production Cloud Deployments

### 1. Dual-Repository Pushing (Optional Git Automation)
To push your clean"

# Supabase Cloud Database Configurations
SUPABASE_URL="https://your-supabase-project.supabase.co"
SUPABASE_KEY="your-supabase-anon-key"

# Resilient Node Provider Gateways (Defaults to Cloudflare fallback local codebase to both your target repositories simultaneously, configure your local remote URLs as follows:
```powershell
git remote remove origin
git remote add origin https://github.com/NomadDigita/AutonomousSmartMoneyTracker.git
git remote set-url --add -- if left empty)
ETH_MAINNET_RPC=https://cloudflare-eth.com

# Dynamic Million-Dollar Gatekeeper Filter (Defaults to 1,000,000 USD)
MIN_ALERT_USD=1000push origin https://github.com/NomadDigita/AutonomousSmartMoneyTracker.git
git remote set-url --add --push origin https://github.com/Themanjoshua/AutonomousSmartMoneyTracker.git000
```

---

## 📡 Interactive Bot Commands

| Menu Button | Command | Action |
| :
```
To stage, commit, and securely push to both repos in a single action, run:
```powershell
git--- | :--- | :--- |
| `🐳 Monitored Wallets` | `/whales` | Connects directly to Blockscout indexer to query current balances and USD values of our tracked smart wallets in real-time. |
| `📈 Bitget Spot Balances` | `/balances` | Authenticates your keys and queries spot assets from your Bitget account. add . ; git commit -m "production: deploy verified clean build modules" ; git push origin master --force
```

### 2 |
| `💡 Narrative Insights` | `/narrative` | Sweeps web narratives via Tavily and rotates Aliyun models to send a styled macro L1 heatmap with detailed text briefing. |
| `📊 Sector Rotations` | `/sectors. Host the Backend on Render.com
*   Create a new **Web Service** on Render pointing to your GitHub repository.
*   **Root Directory**: `backend`
*   **Runtime**: `Node`
*   **Build Command**: `npm install --legacy-peer-deps --production=false && npm run build`
*   **Start Command**: `npm run start` | Gathers trending dex data and rotates Aliyun models to send a styled altcoins volume bubble map with detailed text briefing. |
| `🔔 Enable Alerts` | `/start` | Subscribes your Telegram `chat_id` into Supabase`
*   Add your Environment Variables in the settings panel (matching your local `.env`).

### 3. Host the Frontend on Vercel.com
*   Import your repository on **Vercel** and select ** PostgreSQL to receive instant WebSocket whale notifications. |
| `🔕 Disable Alerts` | `/stop` | Unsubscribes your `frontend`** as the project root directory.
*   Set the framework preset to **Vite**.
*   Add your Environment Variable:`chat_id` from Supabase to disable proactive broadcasts. |
| `⚡ System Status` | `/status` | Outputs active node providers, database latencies, and uptime diagnostics. |
| `ℹ️ Help Guide` | `/help
    *   `VITE_API_BASE_URL` = *Your live production Render URL* (e.g. `https://autonomoussmartmoneytracker-qycg.onrender.com`)
*   Click **Deploy**.

### 4. Enable 24/7 Keep-Alive Autopings
To prevent Render's free tier container from putting your WebSocket poller to sleep:
*   Create a free account at [Cron-Job.org](https://cron-job` | Displays the active help manual. |
| N/A | `/prediction [coin]` | Dynamic macro predictor.org/) or [UptimeRobot](https://uptimerobot.com/).
*   Set up a new **HTTP GET displaying support/resistance trendlines and shaded TradingView-style forecast areas (e.g., `/prediction SOL`). |
| N/A | Monitor** hitting your production gateway health endpoint every 5 minutes:
    `https://autonomoussmartmoneytracker-qycg.onrender.com/health`

---

## 8. License & Hackathon Declarations

SmartFlow AI is open `/watch [address]` | Fetches active liquidity, volume, and fdv parameters for any contract address. |

---

## 📦 Production Deployment Guide

### 1. Backend Web Service Deployment (Render)
* Go to **Render.com** and click **New +** $\rightarrow$ **Web Service**.
* Connect your GitHub repository.
* Set **Root Directory** to `backend`.
-source software licensed under the MIT License. Developed for the **Bitget AI Base Camp · Hackathon S1**, as* Set **Build Command** to `npm install --legacy-peer-deps --production=false && npm run build` (This forces npm to resolve the typescript dependencies under production states cleanly).
* Set **Start Command** to `npm run start`.
* Add your environment keys inside Render's **Environment** variables dashboard.

### 2. Frontend part of the ecosystem co-creation program.

---
```